spring:
  application:
    name: catalog-service

server:
  port: 8080

  datasource:
    url: jdbc:mysql://localhost:3306/bookcatalogdb
    username: root
    password: Luckymydog8080$
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


aws:
  region: us-east-2
  sns:
    livro-criado-topic-arn: ""
    livro-atualizado-topic-arn: ""